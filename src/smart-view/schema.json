{
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the component",
            "$default": {
              "$source": "argv",
              "index": 0
            }
        },
        "prefix": {
            "type": "string",
            "description": "The prefix for the component selector",
            "minLength": 2,
            "maxLength": 3
        }
    },
    "required": ["name"]
}
